<h1>Welcome, <%= @user.first_name %></h1>

<button><%= link_to "Update Profile", edit_user_path(@user) %></button>

<button>
	<label>
	Delete Account
	<% form_tag user_path(@user), method: "delete" do%>
	<%= submit_tag("Delete My Account") %>
	<%end%>
	</label>
</button>